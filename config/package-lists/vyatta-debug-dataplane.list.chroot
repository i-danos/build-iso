# vyatta-debug-dataplane pulls in libdpdk-dbg, which lives only in Debian's
# separate debug-symbol archive (not configured here) rather than the main
# trixie repos. Non-essential debug tooling, so dropped for this build.
#vyatta-debug-dataplane
